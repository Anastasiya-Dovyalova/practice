/* Напишите функцию, которая принимает строку и выводит в консоль
последний символ строки. */

function printsTheLastCharacter(str){
    let x;
    x = str[str.length - 1]
    console.log (x);//и
}
let str = 'Последний символ строки';
