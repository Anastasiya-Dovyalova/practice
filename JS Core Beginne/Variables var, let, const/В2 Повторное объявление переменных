/*Попробуйте объявить переменную с тем же именем дважды, используя var, let и const.
Какие переменные позволяют повторное объявление, а какие нет?*/

const x = 5;
const x;//SyntaxError: Identifier 'x' has already been declared

let y = 6;
let y;//SyntaxError: Identifier 'x' has already been declared

var y = 6;
var y;//нет ошибки
console.log(y);//6

/* Вывод: Если в блоке кода дважды объявить одну и ту же переменную let либо const, то будет ошибка.
Используя var, можно переобъявлять переменную сколько угодно раз. Повторные var игнорируются. 
Если дополнительно присвоить значение, то переменная примет новое значение:*/

var y = 6;
var y = 5;//нет ошибки
console.log(y);//5
